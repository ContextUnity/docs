# Cloudflare Pages configuration for ContextWorker documentation
name = "contextworker-docs"
compatibility_date = "2024-01-01"

# For Cloudflare Pages deployment
# Use: wrangler pages deploy dist --project-name="contextworker-docs" --branch=main
