# Cloudflare Pages configuration for ContextCore documentation
name = "contextcore-docs"
compatibility_date = "2024-01-01"

# For Cloudflare Pages deployment
# Use: wrangler pages deploy dist --project-name="contextcore-docs" --branch=main
